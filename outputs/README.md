# Outputs

Esta pasta deve armazenar artefatos de validação gerados pelo módulo temporal, por exemplo:

- `lime_input_video_temporal_metrics.json`
- `lime_input_video_temporal_metrics_pairs.csv`

Se os arquivos não forem versionados, mantenha esta pasta para preservar a estrutura do projeto.
